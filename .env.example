# ─────────────────────────────────────────────────────────────
# Telegram Claude Code Bot — Configuration
# ─────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Or run the interactive setup instead:
#   bash setup.sh
# ─────────────────────────────────────────────────────────────

# REQUIRED — Your Telegram bot token
# Get one from @BotFather on Telegram: https://t.me/BotFather
# 1. Send /newbot to @BotFather
# 2. Pick a name and username for your bot
# 3. Copy the token (looks like: 123456:ABC-DEF1234ghIkl-zyx57W2v...)
TELEGRAM_BOT_TOKEN=your-bot-token-here

# REQUIRED — Who can use the bot
# Comma-separated Telegram user IDs. Only these users get responses.
# Find your ID: send any message to @userinfobot on Telegram
# Example: 123456789 or 123456789,987654321 for multiple users
ALLOWED_USER_IDS=

# OPTIONAL — Allowed chats/channels where the bot can respond even without from_user ID
# Use numeric Telegram chat IDs (for channels it usually starts with -100...)
# Example: -1001234567890,-1009876543210
ALLOWED_CHAT_IDS=

# Default Claude model (you can switch anytime with /model in the bot)
#   sonnet — Fast and smart (recommended)
#   opus   — Most capable, slower
#   haiku  — Fastest, cheapest
DEFAULT_MODEL=sonnet

# ─────────────────────────────────────────────────────────────
# OPTIONAL — Advanced settings (safe to leave as-is)
# ─────────────────────────────────────────────────────────────

# Directory Claude works in (leave empty for default)
CLAUDE_WORKING_DIR=

# Idle timeout - seconds without output before killing (default: 2 minutes)
IDLE_TIMEOUT=120

# Telegram HTTP timeout for each API request in seconds (default: 90)
TELEGRAM_REQUEST_TIMEOUT_SECONDS=90

# Telegram long-poll timeout for getUpdates in seconds (default: 30)
TELEGRAM_POLLING_TIMEOUT_SECONDS=30

# Polling reconnect backoff settings (defaults: min 1s, max 30s, factor 1.5, jitter 0.1)
TELEGRAM_BACKOFF_MIN_SECONDS=1.0
TELEGRAM_BACKOFF_MAX_SECONDS=30.0
TELEGRAM_BACKOFF_FACTOR=1.5
TELEGRAM_BACKOFF_JITTER=0.1

# Min seconds between progress message updates (default: 3 seconds)
PROGRESS_DEBOUNCE_SECONDS=3.0

# Gemini API guardrail: allow Gemini credentials only for image-generation requests.
# 1=true/enabled (recommended), 0=false/disabled
GEMINI_IMAGE_ONLY_MODE=1

# Port for Prometheus metrics (default: 9101, set to 0 to disable)
METRICS_PORT=9101

# Autonomy loop (background self-learning + proactive failure alerts)
# 1=true/enabled, 0=false/disabled
AUTONOMY_ENABLED=1

# Send proactive alert after this many background task failures in the window
AUTONOMY_FAILURE_THRESHOLD=3
AUTONOMY_FAILURE_WINDOW_MINUTES=60

# Minimum minutes between proactive alerts per chat
AUTONOMY_ALERT_COOLDOWN_MINUTES=30

# Local TTS for voice-bubble replies (voice input -> local STT -> text -> local TTS -> sendVoice)
# Default is espeak. Keep local-only to avoid external API usage.
LOCAL_TTS_BIN=espeak
# Set to "auto" to pick a voice by text script (Cyrillic/Latin).
LOCAL_TTS_VOICE=auto
LOCAL_TTS_VOICE_CYRILLIC=ru
LOCAL_TTS_VOICE_LATIN=en
LOCAL_TTS_ENGINE=auto
LOCAL_TTS_SPEED_WPM=220
LOCAL_TTS_SPEED_WPM_CYRILLIC=170
LOCAL_TTS_SPEED_WPM_LATIN=220
LOCAL_TTS_MAX_CHARS=1200
LOCAL_TTS_VERIFY_INTELLIGIBILITY=1
LOCAL_TTS_MIN_INTELLIGIBILITY_SCORE=0.55
LOCAL_TTS_SHERPA_MIN_INTELLIGIBILITY_SCORE=0.35
LOCAL_TTS_VERIFY_MAX_CHARS=260
LOCAL_TTS_VERIFY_SHERPA=0
LOCAL_TTS_STRICT_CYRILLIC_QUALITY=1
LOCAL_TTS_OPUS_BITRATE=64k
LOCAL_TTS_FFMPEG_AF=
# Optional sherpa-onnx offline TTS (recommended for high-quality Russian voice):
SHERPA_ONNX_RUNTIME_DIR=/home/claude-developer/local/sherpa-onnx-tts/runtime
SHERPA_ONNX_MODEL_DIR=/home/claude-developer/local/sherpa-onnx-tts/models/vits-piper-ru_RU-ruslan-medium
