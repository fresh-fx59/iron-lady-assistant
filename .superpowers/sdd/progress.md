# SDD progress — telegram-aggregator-digest (2026-07-14)
Task 1-3: complete (commits d048555..b8f979a, review clean; Minors for final review: unused pytest import in test file; no tie-breaker on equal-views dedup; progress.md swept into task-2 commit)
Task 4: complete (commits 012a53a+378eeaf fix, re-review clean; Minors for final review: summary-400/401 + exact-12-word overlap boundaries untested, Story unhashability untested)
Task 5-6: complete (commits e873935,45ab661 + fix cfecff2, re-review clean; Minors for final review: summary containing \n bypasses _fit_block 3-line assumption (consider whitespace-normalize in gates parse), headline+links-line alone >cap edge, _connect not explicitly closed, _link_label raw-URL fallback label, BotApiTransport HTTP path untested)
Task 7: complete (commits 2878522 + fix 499f56c, re-review clean; Minors for final review: absent proxy key -> raw RuntimeError traceback from collect; non-dict input doc TypeError past input-error net; no dedicated tests for render-input/status)
Task 8: complete (commit cfbcfd1, review clean; Nits for final review: first run_draft/render-input unguarded (script death, no notify), .gitignore recommendation !.claude/skills/)
Final-review fix wave: SKILL.md rewritten to use $ARGUMENTS only (no $0/$1 indexing ambiguity) — "input file"/"output file"/"feedback file" language, all content rules kept.
Final-review fix wave: aggregator_draft_runner.sh — render-input and both run_draft calls no longer die silently under set -e; rc captured via set +e/set -e, logged, and notify_operator pinged before exit 1.
Final-review fix wave: telegram_aggregator_gates.py _norm_words strips Unicode category-Cf chars (ZWSP U+200B, ZWNJ/ZWJ, soft hyphen) before tokenizing, closing a zero-width bypass of the no-verbatim gate; regression test added (verified it reproduces pre-fix: 4 stories survive instead of 3).
Final-review fix wave: telegram_aggregator_tool.py _cmd_gate now catches TypeError/AttributeError too and explicitly validates input_doc is a dict with a list-of-dicts "posts" before touching it; test covers a top-level JSON array input.
Final-review fix wave: telegram_aggregator_publish.py DigestLedger._connect sets PRAGMA busy_timeout=5000 on every connection.
Final-review fix wave: .gitignore — changed `.claude/` to `.claude/*` + `!.claude/skills/` (a bare `.claude/` directory-prune pattern makes a plain negation inert per git's parent-exclusion rule; verified empirically that `.claude/*` + negation is required for new files under .claude/skills/ to be addable while the rest of .claude/ stays ignored).
Task 9: complete (nix module telegram-aggregator.nix + host import, rebuild OK, LIVE e2e: collect 1554 msgs/13 sources/0 fail; draft unit exit 0 -> 10 stories 0 dropped 2 messages; approve + publish --dry-run rendered correct HTML; publish gated behind publishEnable=false)
